namespace Domain.Enums.Planning;

/// <summary>
/// Confiança de um fluxo projetado: comprometido (alta) vs estimado (média/baixa).
/// </summary>
public enum ConfidenceLevel
{
    High = 1,
    Medium = 2,
    Low = 3
}
